package com.aftermidnight.exception;

public enum ErrorCode {
    USER_NOT_FOUND,
    INVALID_CREDENTIALS,
    UNAUTHORIZED,
    RESOURCE_NOT_FOUND,
    VALIDATION_ERROR,
    INTERNAL_SERVER_ERROR,
    TOO_MANY_REQUESTS
}
